### .mod for the skeleton flagswitch
### skeleton by MEEEB
name = "Skeleton Flagswitch" # Change the name to something else
path = "mod/skeleton_flagswitch" # change skeleton_flagswitch to whatever you renamed the folder to
#dependencies = { "name_of_dependency_as_shown_in_the_launcher" } # uncomment and change this only if something should explicitly be loaded before your mod (this is different from dependencies shown in the workshop)
tags = { "skeleton" "cosmetic" } # change them to your liking
supported_version = "*.*" # *.* means every version is supported
#picture = "path_to_pic_inside_[path]_directory" # if you have a logo, uncomment this and do what the text says
